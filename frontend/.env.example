# ============================================
# FIREBASE CONFIGURATION (CLIENT-SIDE - PUBLIC)
# ============================================
# These variables are prefixed with NEXT_PUBLIC_ which means they are 
# exposed to the browser. This is INTENTIONAL and SAFE for Firebase.
# Firebase security is enforced by Firestore Security Rules, not by 
# hiding these values.

NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain_here
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id_here
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket_here
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id_here

# ============================================
# GOOGLE GEMINI API (SERVER-SIDE ONLY - SECRET)
# ============================================
# DO NOT add NEXT_PUBLIC_ prefix to this variable!
# This key is used ONLY on the server side and should NEVER be exposed
# to the browser. It's used in /api routes which run on the server.

GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# OPTIONAL CONFIGURATION
# ============================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit .env.local to git (already in .gitignore)
# 2. Firebase config IS visible in browser - this is normal
# 3. GEMINI_API_KEY must NEVER have NEXT_PUBLIC_ prefix
# 4. Always deploy Firestore security rules to protect your data
# 5. Configure domain restrictions in Firebase Console
# 6. Enable Firebase App Check for additional security
